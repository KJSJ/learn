(function () {
	'use strict';

	angular
		.module('alpha.accounts', [
			'alpha.accounts.controllers',
			'alpha.accounts.services'
		]);

	angular
		.module('alpha.accounts.controllers', []);

	angular
		.module('alpha.accounts.services', []);
})();